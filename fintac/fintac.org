#+begin_src latex
  \section{\ty{fintac}}
  The program \ty{fintac} takes as input a Newick tree and finds the
  target clade. In the input tree the leaf labels need to distinguish
  taxonomic targets from taxonomic neighbors and nodes whose status is
  unknown. In addition, the internal nodes of the input tree have unique
  labels. The program then looks for the node that maximizes the split
  between targets and neighbors.

  To find this node, let $n_{\rm t}$, $n_{\rm n}$, and $n_{\rm u}$ be
  the number of taxonomic targets, neighbors, and unknowns. We further
  call $v_{\rm t}$, $v_{\rm n}$, and $v_{\rm u}$ the number of targets,
  neighbors, and unknowns in the clade rooted on $v$. If the taxonomy
  always agreed with the phylogeny, we could just look for the node that
  contains all targets and no neighbors or unknowns, $v_{\rm t}=n_{\rm
    t}$ and $v_{\rm n}=v_{\rm u}=0$. But if taxonomies and phylogenies
  always agreed, we could restrict our search for targets and neighbors
  to the output of \ty{neighbors}.

  As is well-known, there is often no phylogenetic node that splits the
  sample into pure taxonomic targets and neighbors. So we further define
  $v^{\ast}_{\rm n}$ as the number of neighbors in $v$'s neighborhood,
  that is, the number of neighbors \emph{outside} of $v$. Similarly,
  $v^{\ast}_{\rm u}$ is the number of unknowns outside of $v$. Now we
  can look for the node that maximizes the percent split between targets
  on the one hand, and neighbors and unknowns on the other.

  To quantify the contribution of unknowns, we note that positive
  identification as target or neighbor is much stronger information than
  ``unknown''. So we enter the counts of targets and neighbors linearly,
  while the counts of unknowns are entered logarithmically to get our
  split score,
  \begin{equation}\label{eq:spl}
    s(v)=\frac{v_{\rm t} + v^{\ast}_{\rm n} +\log(v^{\ast}_{\rm
	u}+1)}{n_{\rm n} + n_{\rm t} + \log(n_{\rm u}+1)}\times 100.
  \end{equation}
  This quantity ranges from 0, if $v$ contains no targets and all
  neighbors and unknowns, to 100, if $v$ contains all targets and no
  neighbors or unknowns.
  \subsection*{Implementation}
  The outline of \ty{fintac} has hooks for imports, types, functions,
  and the logic of the main function.  \bpr{fintac}{pr:fit}
#+end_src
#+begin_src go <<fintac.go>>=
  package main

  import (
	  //<<Imports, Pr. \ref{pr:fit}>>
  )
  //<<Types, Pr. \ref{pr:fit}>>
  //<<Functions, Pr. \ref{pr:fit}>>
  func main() {
	  //<<Main function, Pr. \ref{pr:fit}>>
  }
#+end_src
#+begin_src latex
  \epr Inside the main function we set the name of \ty{fintac} and its
  usage, declare its options, parse them, and respond to them. Then we
  parse the input files.
#+end_src
#+begin_src go <<Main function, Pr. \ref{pr:fit}>>=
  util.SetName("fintac")
  //<<Set usage, Pr. \ref{pr:fit}>>
  //<<Declare options, Pr. \ref{pr:fit}>>
  //<<Parse options, Pr. \ref{pr:fit}>>
  //<<Respond to options, Pr. \ref{pr:fit}>>
  //<<Parse input files, Pr. \ref{pr:fit}>>
#+end_src
#+begin_export latex
We import \ty{util}.
#+end_export
#+begin_src go <<Imports, Pr. \ref{pr:fit}>>=
  "github.com/evolbioinf/neighbors/util"
#+end_src
#+begin_src latex
The usage consists of three parts, the actual usage message, an
explanation of the purpose of \ty{fintac}, and an example command.
#+end_src
#+begin_src go <<Set usage, Pr. \ref{pr:fit}>>=
  u := "fintac [option]... [foo.nwk]..."
  p := "Find target clade in Newick tree."
  e := "fintac foo.nwk"
  clio.Usage(u, p, e)
#+end_src
#+begin_src latex
We import \ty{clio}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:fit}>>=
  "github.com/evolbioinf/clio"
#+end_src
#+begin_src latex
  Apart from the obligatory version, \ty{-v}, we declare an option for
  listing all splits, instead of only the default maximal splits. We
  also declare an option each for the target, neighbor, and unknown
  regular expressions. The target regular expression is initialized to
  prefix \ty{t}. The neighbor regular expression is initialized to
  nothing, which we interpret as anything that is neither a target nor
  unknown.
#+end_src
#+begin_src go <<Declare options, Pr. \ref{pr:fit}>>=
  optV := flag.Bool("v", false, "version")
  optA := flag.Bool("a", false, "all splits (default maximal)")
  optT := flag.String("t", "^t", "target regex")
  optU := flag.String("u", "", "unknown regex")
  optN := flag.String("n", "", "neighbor regex " +
	  "(default complement of -t and -u)")
#+end_src
#+begin_src latex
We import \ty{flag}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:fit}>>=
  "flag"
#+end_src
#+begin_src latex
We parse the options.
#+end_src
#+begin_src go <<Parse options, Pr. \ref{pr:fit}>>=
  flag.Parse()
#+end_src
#+begin_src latex
  We respond to the version, target, neighbor, and unknown options.
#+end_src
#+begin_src go <<Respond to options, Pr. \ref{pr:fit}>>=
  //<<Respond to \ty{-v}, Pr. \ref{pr:fit}>>
  //<<Respond to \ty{-t}, Pr. \ref{pr:fit}>>
  //<<Respond to \ty{-n}, Pr. \ref{pr:fit}>>
  //<<Respond to \ty{-u}. Pr. \ref{pr:fit}>>
#+end_src
#+begin_src latex
  If the user asked for the version, we print it as part of the program
  info.
#+end_src
#+begin_src go <<Respond to \ty{-v}, Pr. \ref{pr:fit}>>=
  if *optV {
	  util.PrintInfo("fintac")
  }
#+end_src
#+begin_src latex
  We compile the target regex and check the error returned.
#+end_src
#+begin_src go <<Respond to \ty{-t}, Pr. \ref{pr:fit}>>=
  tregex, err := regexp.Compile(*optT)
  util.Check(err)
#+end_src
#+begin_src latex
  We import \ty{regexp}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:fit}>>=
  "regexp"
#+end_src
#+begin_src latex
  The neighbor regex is \ty{nil}, unless the user set one.
#+end_src
#+begin_src go <<Respond to \ty{-n}, Pr. \ref{pr:fit}>>=
  var nregex *regexp.Regexp
  if *optN != "" {
	  nregex, err = regexp.Compile(*optN)
	  util.Check(err)
  }
#+end_src
#+begin_export latex
Similarly, the unknown regex is \ty{nil} unless set by the user.
#+end_export
#+begin_src go <<Respond to \ty{-u}. Pr. \ref{pr:fit}>>=
  var uregex *regexp.Regexp
  if *optU != "" {
	  uregex, err = regexp.Compile(*optU)
	  util.Check(err)
  }
#+end_src
#+begin_src latex
  The remaining tokens on the command line are interpreted as the names
  of input files. These are passed to the function \ty{ParseFiles},
  which applies the function \ty{parse} to each file. The function
  \ty{parse}, in turn, takes as arguments the option \ty{-a}, and the
  target, neighbor, and unknown regular expressions.
#+end_src
#+begin_src go <<Parse input files, Pr. \ref{pr:fit}>>=
  files := flag.Args()
  clio.ParseFiles(files, parse, optA, tregex, nregex, uregex)
#+end_src
#+begin_src latex
  Inside \ty{parse}, we retrieve its arguments, iterate over the trees
  inside the current file, and analyze each tree.
#+end_src
#+begin_src go <<Functions, Pr. \ref{pr:fit}>>=
  func parse(r io.Reader, args ...interface{}) {
	  //<<Retrieve arguments, Pr. \ref{pr:fit}>>
	  sc := nwk.NewScanner(r)
	  for sc.Scan() {
		  tree := sc.Tree()
		  //<<Analyze tree, Pr. \ref{pr:fit}>>
	  }
  }
#+end_src
#+begin_src latex
We import \ty{io} and \ty{nwk}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:fit}>>=
  "io"
  "github.com/evolbioinf/nwk"
#+end_src
#+begin_export latex
We retrieve the four arguments we passed, printing all, and the three
regular expressions.
#+end_export
#+begin_src go <<Retrieve arguments, Pr. \ref{pr:fit}>>=
  optA := args[0].(*bool)
  tregex := args[1].(*regexp.Regexp)
  nregex := args[2].(*regexp.Regexp)
  uregex := args[3].(*regexp.Regexp)
#+end_src
#+begin_src latex
To analyze a tree, we count nodes, process the counts, and print
them.
#+end_src
#+begin_src go <<Analyze tree, Pr. \ref{pr:fit}>>=
  //<<Count nodes, Pr. \ref{pr:fit}>>
  //<<Process counts, Pr. \ref{pr:fit}>>
  //<<Print counts, Pr. \ref{pr:fit}>>
#+end_src
#+begin_src latex
  We traverse the tree by calling the function \ty{traverseTree}, which
  takes as arguments a map, and the target, neighbor, and unknown
  regular expressions.
#+end_src
#+begin_src go <<Count nodes, Pr. \ref{pr:fit}>>=
  counts := make(map[int]*Count)
  traverseTree(tree, counts, tregex, nregex, uregex)
#+end_src
#+begin_src latex
  A count is understood per node; so it consists of the node label, the
  label of its parent, the number of neighbors, targets, and unknowns in
  its subtree, its split as defined in equation~(\ref{eq:spl}), and its
  distance to the parent.
#+end_src
#+begin_src go <<Types, Pr. \ref{pr:fit}>>=
  type Count struct {
	  label, parent string
	  vn, vt, vu int
	  sv, dp float64
  }
#+end_src
#+begin_src latex
  The function \ty{traverseTree} recursively traverses the tree rooted
  on its input node. For each node it adds a count preorder and analyzes
  the node postorder.
#+end_src
#+begin_src go <<Functions, Pr. \ref{pr:fit}>>=
  func traverseTree(v *nwk.Node, counts map[int]*Count,
	  tregex, nregex, uregex *regexp.Regexp) {
	  if v == nil {
		  return
	  }
	  //<<Add count, Pr. \ref{pr:fit}>>
	  traverseTree(v.Child, counts, tregex, nregex, uregex)
	  traverseTree(v.Sib, counts, tregex, nregex, uregex)
	  //<<Analyze $v$, Pr. \ref{pr:fit}>>
  }
#+end_src
#+begin_src latex
We create and store a count for each node. In the count we
store the label of $v$ and, if $v$ isn't the root, also the label of
its parent and the distance to it.
#+end_src
#+begin_src go <<Add count, Pr. \ref{pr:fit}>>=
  count := new(Count)
  count.label = v.Label
  if v.Parent != nil {
	  count.dp = v.Length
	  count.parent = v.Parent.Label
  }
  counts[v.Id] = count
#+end_src
#+begin_src latex
  If the current node is a leaf, we check its label, to make sure it is
  either a target, a neighbor, or an unknown. If the current node is not
  a leaf, it is an internal node. In that case we also check its label
  to make sure it actually has one. In either case we count the targets,
  neighbors, and unknowns.
#+end_src
#+begin_src go <<Analyze $v$, Pr. \ref{pr:fit}>>=
  if v.Child == nil {
	  //<<Check leaf label, Pr. \ref{pr:fit}>>
  } else {
	  //<<Check internal node label, Pr. \ref{pr:fit}>>
  }
  //<<Count targets and neighbors, Pr. \ref{pr:fit}>>
#+end_src
#+begin_src latex
  When checking the leaf label, we first determine its type, target,
  neighbor, or unknown. Then we ensure we are dealing either with a
  target, a neighbor, or an unknown, and finally count the leaf type.
#+end_src
#+begin_src go <<Check leaf label, Pr. \ref{pr:fit}>>=
  //<<Determine leaf type, Pr. \ref{pr:fit}>>
  //<<Ensure either target, neighbor, or unknown, Pr. \ref{pr:fit}>>
  //<<Count leaf type, Pr. \ref{pr:fit}>>
#+end_src
#+begin_src latex
  We declare three indicator variables for the leaf type and determine
  whether the leaf is a target, an unknown, or a neighbor.
#+end_src
#+begin_src go <<Determine leaf type, Pr. \ref{pr:fit}>>=
  isTar := false
  isUnk := false
  isNei := false
  //<<Is the leaf a target? Pr. \ref{pr:fit}>>
  //<<Is the leaf an unknown? Pr. \ref{pr:fit}>>
  //<<Is the leaf a neighbor? Pr. \ref{pr:fit}>>
#+end_src
#+begin_src latex
  If the leaf label matches the target regex, it is a target.
#+end_src
#+begin_src go <<Is the leaf a target? Pr. \ref{pr:fit}>>=
  if tregex.MatchString(v.Label) {
	  isTar = true
  }
#+end_src
#+begin_export latex
Similarly, if the leaf matches the unknown regex, it is a complete
unknown.
#+end_export
#+begin_src go <<Is the leaf an unknown? Pr. \ref{pr:fit}>>=
  if uregex != nil && uregex.MatchString(v.Label) {
	  isUnk = true
  }
#+end_src
#+begin_src latex
  If the user didn't set a neighbor regex, the rule is that anything
  that isn't a target or an unknown, is a neighbor. If, however, the
  user \emph{did} set a neighbor regex, a match to it makes the leaf a
  neighbor.
#+end_src
#+begin_src go <<Is the leaf a neighbor? Pr. \ref{pr:fit}>>=
  if nregex == nil {
	  if !isTar && !isUnk {
		  isNei = true
	  }
  } else if nregex.MatchString(v.Label) {
	  isNei = true
  }
#+end_src
#+begin_src latex
  At this point our leaf might be any combination of target, neighbors,
  and unknown. We want to ensure it is exactly one of the three. So we
  count the designations and then respond to the count.
#+end_src
#+begin_src go <<Ensure either target, neighbor, or unknown, Pr. \ref{pr:fit}>>=
  //<<Count designations, Pr. \ref{pr:fit}>>
  //<<Respond to designation count, Pr. \ref{pr:fit}>>
#+end_src
#+begin_export latex
We count the designations of the current leaf by going over the
indicator variables for target, neighbor, and unknown.
#+end_export
#+begin_src go <<Count designations, Pr. \ref{pr:fit}>>=
  dc := 0
  if isTar {
	  dc++
  }
  if isUnk {
	  dc++
  }
  if isNei {
	  dc++
  }
#+end_src
#+begin_export latex
If the leaf has zero designations, we warn the user. If it has more
than one designation, something has gone wrong, and we exit with a
friendly message.
#+end_export
#+begin_src go <<Respond to designation count, Pr. \ref{pr:fit}>>=
  if dc == 0 {
	  fmt.Fprintf(os.Stderr, "WARNING[fintac]: %q " +
		  "is neither target, neighbor, nor " +
		  "unknown\n",
		  v.Label)
  }
  if dc > 1 {
	  log.Fatalf("%q is ambiguous: t %t, n %t, u %t",
		  v. Label, isTar, isNei, isUnk)
  }
#+end_src
#+begin_src latex
  We import \ty{fmt}, \ty{os}, and \ty{log}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:fit}>>=
  "fmt"
  "os"
  "log"
#+end_src
#+begin_src latex
  We have now carried out enough tests to confidently count the leaf
  type.
#+end_src
#+begin_src go <<Count leaf type, Pr. \ref{pr:fit}>>=
  if isTar {
	  counts[v.Id].vt = 1
  } else if isNei {
	  counts[v.Id].vn = 1
  } else if isUnk {
	  counts[v.Id].vu = 1
  }
#+end_src
#+begin_src latex
We make sure all internal nodes are labeled.
#+end_src
#+begin_src go <<Check internal node label, Pr. \ref{pr:fit}>>=
  if v.Label == "" {
	  log.Fatal("please label internal nodes " +
		  "using land")
  }
#+end_src
#+begin_src latex
  We count the targets, neighbors, and unknowns by incrementing the
  corresponding counter in the parent---unless we're a the root.
#+end_src
#+begin_src go <<Count targets and neighbors, Pr. \ref{pr:fit}>>=
  if v.Parent != nil {
	  counts[v.Parent.Id].vt += counts[v.Id].vt
	  counts[v.Parent.Id].vn += counts[v.Id].vn
	  counts[v.Parent.Id].vu += counts[v.Id].vu
  }
#+end_src
#+begin_src latex
We now have the raw counts in hand. From these we calculate the splits
and sort the counts.
#+end_src
#+begin_src go <<Process counts, Pr. \ref{pr:fit}>>=
  //<<Calculate splits, Pr. \ref{pr:fit}>>
  //<<Sort counts, Pr. \ref{pr:fit}>>
#+end_src
#+begin_src latex
  We calculate splits according to equation~(\ref{eq:spl}). First, we
  determine the total counts of targets, neighbors, and unknowns,
  $n_{\rm t}$, $n_{\rm n}$, and $n_{\rm u}$. These are the counts of
  targets, neighbors, and unknowns for the root.
#+end_src
#+begin_src go <<Calculate splits, Pr. \ref{pr:fit}>>=
  nt := float64(counts[tree.Id].vt)
  nn := float64(counts[tree.Id].vn)
  nu := float64(counts[tree.Id].vu)
#+end_src
#+begin_src latex
  We iterate over the counts. For each count we calculate the
  number of neighbors and unknowns in the neighborhood,
  \begin{eqnarray*}
  v^{\ast}_{\rm n} & = & n_{\rm n} - v_{\rm n}\\
  v^{\ast}_{\rm u} & = & n_{\rm u} - v_{\rm u}
  \end{eqnarray*}
  Then we apply equation~(\ref{eq:spl}) to compute the split percentage.
#+end_src
#+begin_src go <<Calculate splits, Pr. \ref{pr:fit}>>=
  for _, count := range counts {
	  van := nn - float64(count.vn)
	  vau := nu - float64(count.vu)
	  vt := float64(count.vt)
	  count.sv = (vt + van + math.Log(vau+1.0)) /
		  (nt + nn + math.Log(nu+1.0)) * 100.0
  }
#+end_src
#+begin_export latex
We import \ty{math}.
#+end_export
#+begin_src go <<Imports, Pr. \ref{pr:fit}>>=
  "math"
#+end_src
#+begin_src latex
  We sort the counts by converting them to a slice and sorting that by
  the split score. If the split scores are equal, we disambiguate by the
  label.
#+end_src
#+begin_src go <<Sort counts, Pr. \ref{pr:fit}>>=
  cs := make([]*Count, 0)
  for _, count := range counts {
	  cs = append(cs, count)
  }
  slices.SortFunc(cs, func(a, b *Count) int {
	  if a.sv < b.sv {
		  return 1
	  } else if a.sv > b.sv {
		  return -1
	  }
	  return strings.Compare(a.label, b.label)
  })
#+end_src
#+begin_src latex
  We import \ty{slices} and \ty{strings}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:fit}>>=
  "slices"
  "strings"
#+end_src
#+begin_src latex
  We print the counts, or rather, the maximal splits, in a table, which
  we construct using a tab writer. If the user asked for all splits, we
  also print the rest.
#+end_src
#+begin_src go <<Print counts, Pr. \ref{pr:fit}>>=
  w := tabwriter.NewWriter(os.Stdout, 0, 0, 2, ' ', 0)
  //<<Print maximal splits, Pr. \ref{pr:fit}>>
  if *optA {
	  //<<Print remaining splits, Pr. \ref{pr:fit}>>
  }
  w.Flush()
#+end_src
#+begin_src latex
We import \ty{tabwriter}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:fit}>>=
  "text/tabwriter"
#+end_src
#+begin_src latex
  We print the splits in seven columns, clade, targets, neighbors,
  unknowns, parent, distance to parent, and split. We print the headers
  for these seven columns and then the maximal splits. These all have
  the same split percentage as the top scorer. The root has no parent,
  so we replace the root's parent label with a hyphen.
#+end_src
#+begin_src go <<Print maximal splits, Pr. \ref{pr:fit}>>=
  fmt.Fprint(w, "#Clade\tTargets\tNeighbors\tUnknowns\t" +
	  "Split (%)\tParent\tDist(Parent)\n")
  i := 0
  for ; i < len(cs) && cs[0].sv == cs[i].sv; i++ {
	  pl := cs[i].parent
	  if pl == "" {
		  pl = "-"
	  }
	  fmt.Fprintf(w, "%s\t%d\t%d\t%d\t%.2f\t%s\t%g\n",
		  cs[i].label, cs[i].vt, cs[i].vn, cs[i].vu,
		  cs[i].sv, pl, cs[i].dp)
  }
#+end_src
#+begin_src latex
We print the remaining splits.
#+end_src
#+begin_src go <<Print remaining splits, Pr. \ref{pr:fit}>>=
  for ; i < len(cs); i++ {
	  pl := cs[i].parent
	  if pl == "" {
		  pl = "-"
	  }
	  fmt.Fprintf(w, "%s\t%d\t%d\t%d\t%.2f\t%s\t%g\n",
		  cs[i].label, cs[i].vt, cs[i].vn, cs[i].vu,
		  cs[i].sv, pl, cs[i].dp)
  }
#+end_src
#+begin_src latex
We have finished writing \ty{fintac}, so let's test it.
\subsection*{Testing}
Our code for testing \ty{fintac} contains hooks for imports and the
testing logic.
#+end_src
#+begin_src go <<fintac_test.go>>=
  package main

  import (
	  "testing"
	  //<<Testing imports, Pr. \ref{pr:fit}>>
  )

  func TestFintac(t *testing.T) {
	  //<<Testing, Pr. \ref{pr:fit}>>
  }
#+end_src
#+begin_src latex
We construct a set of tests and run them.
#+end_src
#+begin_src go <<Testing, Pr. \ref{pr:fit}>>=
  var tests []*exec.Cmd
  //<<Construct tests, Pr. \ref{pr:fit}>>
  for i, test := range tests {
	  //<<Run test, Pr. \ref{pr:fit}>>
  }
#+end_src
#+begin_src latex
We import \ty{exec}.
#+end_src
#+begin_src go <<Testing imports, Pr. \ref{pr:fit}>>=
  "os/exec"
#+end_src
#+begin_src latex
  We run all our tests on the same tree, \ty{test.nwk}. Our first test
  analyzes this tree with no options set.
#+end_src
#+begin_src go <<Construct tests, Pr. \ref{pr:fit}>>=
  f := "./test.nwk"
  p := "./fintac"
  test := exec.Command(p, f)
  tests = append(tests, test)
#+end_src
#+begin_src latex
In our second and last test we request that all splits be printed.
#+end_src
#+begin_src go <<Construct tests, Pr. \ref{pr:fit}>>=
  test = exec.Command(p, "-a", f)
  tests = append(tests, test)
#+end_src
#+begin_export latex
We test the target regex.
#+end_export
#+begin_src go <<Construct tests, Pr. \ref{pr:fit}>>=
  test = exec.Command(p, "-t", "^tGC[AF]", f)
  tests = append(tests, test)
#+end_src
#+begin_export latex
We test the neighbors regex.
#+end_export
#+begin_src go <<Construct tests, Pr. \ref{pr:fit}>>=
  test = exec.Command(p, "-n", "^nGC[AF]", f)
  tests = append(tests, test)
#+end_src
#+begin_export latex
We test the ``unknown'' option by calling all neighbors ``unknowns''.
#+end_export
#+begin_src go <<Construct tests, Pr. \ref{pr:fit}>>=
  test = exec.Command(p, "-u", "^n", f)
  tests = append(tests, test)
#+end_src
#+begin_src latex
For a given test we compare the results we get with the results we
want, which are stored in files \ty{r1.txt} and \ty{r2.txt}.
#+end_src
#+begin_src go <<Run test, Pr. \ref{pr:fit}>>=
  get, err := test.Output()
  if err != nil {
	  t.Error(err)
  }
  name := "r" + strconv.Itoa(i+1) + ".txt"
  want, err := os.ReadFile(name)
  if err != nil {
	  t.Error(err)
  }
  if !bytes.Equal(get, want) {
	  t.Errorf("get:\n%s\nwant:\n%s\n", get, want)
  }
#+end_src
#+begin_src latex
We import \ty{strconv}, \ty{os}, and \ty{bytes}.
#+end_src
#+begin_src go <<Testing imports, Pr. \ref{pr:fit}>>=
  "strconv"
  "os"
  "bytes"
#+end_src
