#+begin_src latex
  \section*{Introduction}
    The program \ty{dree} takes as input a taxon-ID and the name of the
    Neighbors database. It returns the subtree rooted on the focal
    taxon. If the focal taxon is close to the root, the subtree might be
    very large, so the user can set the maximum level considered. The
    root of the subtree has level zero, its children level one, and so
    on.

  The subtree is written in the dot language ready for rendering with
  \ty{dot}, which is part of the graphviz package. For example, we can
  draw the tree for the \emph{Homininae}, taxon-ID 207598, which include
  human, chimp, and gorilla. By default, \ty{dree} labels taxa with
  their IDs, but with \ty{-n} we get names instead. Taxa with sequenced
  genomes are colored.
#+end_src
#+begin_src sh <<dree.sh>>=
  dree -n 207598 test.db | xdot -
#+end_src
#+begin_src latex
  The resulting tree in Figure~\ref{fig:homininaeFull} is pretty
  crowded, so we use \ty{-g} to to reduce it to the taxa with sequenced
  genomes and their ancestors, which gives the more legible
  Figure~\ref{fig:homininaeG}.
#+end_src
#+begin_src sh <<dree.sh>>=
  dree -n -g 207598 test.db | xdot -
#+end_src
#+begin_src latex
  \begin{figure}
    \begin{center}
  	\resizebox{\textwidth}{!}{\includegraphics{../dree/homininae_full_n}}
    \end{center}
    \caption{Taxonomy of the \emph{Homininae}, taxa with sequenced
  	genomes are colored.}\label{fig:homininaeFull}
  \end{figure}

  \begin{figure}
    \begin{center}
  	\resizebox{\textwidth}{!}{\includegraphics{../dree/homininae_g_n}}
    \end{center}
    \caption{Taxonomy of the \emph{Homininae} reduced to taxa with
  	sequenced genomes and their ancestors; taxa with genomes are
  	colored.}\label{fig:homininaeG}
  \end{figure}

  Instead of drawing a taxonomic tree, the user can list the taxa with
  \ty{-l}. The resulting table has by default four columns, taxon-ID,
  parent, rank, and number of genomes. A fifth column of names is
  activated with \ty{-n}. For example, we can list the eight taxa with
  genomes in Figure~\ref{fig:homininaeG}.
#+end_src
#+begin_src sh <<dree.sh>>=
  dree -n -g -l 207598 test.db
#+end_src
#+begin_export latex
\begin{verbatim}
# Taxid  Parent  Rank        Genomes  Name
9593     9592    species     2        Gorilla gorilla
9595     9593    subspecies  12       Gorilla gorilla gorilla
9597     9596    species     7        Pan paniscus
9598     9596    species     12       Pan troglodytes
9606     9605    species     1851     Homo sapiens
37012    9598    subspecies  1        Pan troglodytes verus
499232   9592    species     1        Gorilla beringei
1159185  499232  subspecies  2        Gorilla beringei beringei
\end{verbatim}
#+end_export
#+begin_export latex
By default, the number of genomes is the raw count. We can also list
the recursive count, that is, the number of genomes in a subtree
rooted on a given taxon.
#+end_export
#+begin_src sh <<dree.sh>>=
  dree -R -n -g -l 207598 test.db
#+end_src
#+begin_export latex
\begin{verbatim}
# Taxid  Parent  Rank        Genomes  Name
9592     207598  genus       17       Gorilla
9593     9592    species     14       Gorilla gorilla
9595     9593    subspecies  12       Gorilla gorilla gorilla
9596     207598  genus       20       Pan
9597     9596    species     7        Pan paniscus
9598     9596    species     13       Pan troglodytes
9605     207598  genus       1851     Homo
9606     9605    species     1851     Homo sapiens
37012    9598    subspecies  1        Pan troglodytes verus
207598   207598  subfamily   1888     Homininae
499232   9592    species     3        Gorilla beringei
1159185  499232  subspecies  2        Gorilla beringei beringei
\end{verbatim}
By default, the number of genomes listed is the sum over all assembly
levels. If we reduce the assembly level to ``complete'', the number of
genomes available drops to 58.
#+end_export
#+begin_src sh <<dree.sh>>=
  dree -L complete -R -n -g -l 207598 test.db
#+end_src
#+begin_export latex
\begin{verbatim}
# Taxid  Parent  Rank       Genomes  Name
9605     207598  genus      58       Homo
9606     9605    species    58       Homo sapiens
207598   207598  subfamily  58       Homininae
\end{verbatim}

\section*{Implementation}
The outline of \ty{dree} has hooks for imports and the logic of the
run function.  \bpr{dree}{pr:dr}
#+end_export
#+begin_src go <<dree.go>>=
  package dree

  import (
	  //<<Imports, Pr. \ref{pr:dr}>>
  )

  func Run() {
	  //<<Run function, Pr. \ref{pr:dr}>>
  }
#+end_src
#+begin_src latex
  \epr In the run function we set the name of \ty{dree} and its usage,
  declare the options, and parse them. Then we get the subtree we're
  after, annotate it, and print it.
#+end_src
#+begin_src go <<Run function, Pr. \ref{pr:dr}>>=
  util.SetName("dree")
  //<<Set usage, Pr. \ref{pr:dr}>>
  //<<Declare options, Pr. \ref{pr:dr}>>
  //<<Parse options, Pr. \ref{pr:dr}>>
  //<<Get subtree, Pr. \ref{pr:dr}>>
  //<<Annotate subtree, Pr. \ref{pr:dr}>>
  //<<Draw subtree, Pr. \ref{pr:dr}>>
#+end_src
#+begin_export latex
We import \ty{util}.
#+end_export
#+begin_src go <<Imports, Pr. \ref{pr:dr}>>=
  "github.com/evolbioinf/neighbors/util"
#+end_src
#+begin_src latex
  The usage consists of the actual usage message, an explanation of the
  purpose of \ty{dree}, and an example command, where we include code
  for rendering the taxonomy.
#+end_src
#+begin_src go <<Set usage, Pr. \ref{pr:dr}>>=
  u := "dree [-h] [option]... <taxon-ID> <db>"
  p := "Get the taxonomy rooted on a specific taxon."
  e := "dree -n -g 207598 neidb | xdot -"
  clio.Usage(u, p, e)
#+end_src
#+begin_src latex
  We import \ty{clio}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:dr}>>=
  "github.com/evolbioinf/clio"
#+end_src
#+begin_src latex
  Apart from the version, we declare eight options:
  \begin{itemize}
  \item \ty{-n}: print \emph{names} instead of taxon-IDs
  \item \ty{-g}: print only taxa with \emph{genomes}
  \item \ty{-l}: \emph{list} taxa
  \item \ty{-L}: only consider genomes with a particular assembly
    \emph{level}
  \item \ty{-m}: maximum tree level
  \item \ty{-R}: \emph{recursive} genome counts in list
  \item \ty{-r}: execute program on remote server
  \item \ty{-D}: use a specific databas for remote execution
  \end{itemize}
#+end_src
#+begin_src go <<Declare options, Pr. \ref{pr:dr}>>=
  optV := flag.Bool("v", false, "version")
  optN := flag.Bool("n", false,
  	  "print names instead of taxon-IDs")
  optG := flag.Bool("g", false,
  	  "only taxa with genome sequences")
  optL := flag.Bool("l", false, "list taxa")
  optLL := flag.String("L", "", util.LevelMsg())
  optM := flag.Int("m", -1, "maximum tree level")
  optRR := flag.Bool("R", false, "recursive genome counts in list " +
  	"(default raw counts)")
  optR := flag.Bool("r", false, "remote execution (implies db)")
  optDD := flag.String("D", "", "name of remote database (implies remote execution)")
#+end_src
#+begin_src latex
  We import \ty{flag}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:dr}>>=
  "flag"
#+end_src
#+begin_src latex
  We parse the options and respond to the version option, \ty{-v},
  the remote switch, \ty{-r}, the assembly levels option, \ty{-L},
  and the maximum tree levels option,  \ty{-m}. Then we get the
  taxon-ID and the database name. Using the database name, we
  open the database connection.
#+end_src
#+begin_src go <<Parse options, Pr. \ref{pr:dr}>>=
  flag.Parse()
  //<<Respond to \ty{-v}, Pr. \ref{pr:dr}>>
  //<<Respond to \ty{-r}, Pr. \ref{pr:dr}>>
  //<<Respond to \ty{-L}, Pr. \ref{pr:dr}>>
  //<<Respond to \ty{-m}, Pr. \ref{pr:dr}>>
  //<<Get taxon-ID, Pr. \ref{pr:dr}>>
  //<<Get database name, Pr. \ref{pr:dr}>>
  //<<Open database connection, Pr. \ref{pr:dr}>>
#+end_src
#+begin_src latex
  If the user requested the version, we print the program information,
  which contains the version.
#+end_src
#+begin_src go <<Respond to \ty{-v}, Pr. \ref{pr:dr}>>=
  if *optV {
	  util.PrintInfo("dree")
  }
#+end_src
#+begin_src latex
  We import \ty{util}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:dr}>>=
  "github.com/evolbioinf/neighbors/util"
#+end_src
#+begin_src latex
  If a remote database is given, we pass all the call arguments to
  \ty{SendGetRequest} and print its response. We remove the options \ty{-r} and
  \ty{-D} in sanitization.
#+end_src
#+begin_src go <<Respond to \ty{-r}, Pr. \ref{pr:dr}>>=
  if *optR || *optDD != "" {
    //<<Set misc arguments, Pr. \ref{pr:neig}>>
    options := []util.Option{{Name: "r", WithValue: false}, {Name: "D", WithValue: true}}
		resp := util.SendGetRequest(
			"api/v2/programs/dree",
			util.SanitizeArguments(os.Args[1:], options),
      []string{},
      misc,
		)
		fmt.Print(resp)
		return
  }
#+end_src
#+begin_export latex
  Miscellaneous are anything that does not belong to a normal/ local program
  call. In this case it's the specification of a database.
#+end_export
#+begin_src go <<Set misc arguments, Pr. \ref{pr:neig}>>=
  misc := map[string]string{}
  if *optDD != "" {
    misc["db"] = *optDD
  }
#+end_src
#+begin_src latex
  In order to respond to the levels option, we get the map of known
  levels, \ty{knowns}. We also declare a map of desired levels. Then we
  obtain the requested levels from the \ty{-L} option. If the user
  requested one or more levels, we iterate over them. Otherwise, the
  known levels become the desired levels.
#+end_src
#+begin_src go <<Respond to \ty{-L}, Pr. \ref{pr:dr}>>=
  levels := make(map[string]bool)
  knowns := make(map[string]bool)
  //<<Fill in known levels, Pr. \ref{pr:dr}>>
  var requests []string
  if *optLL != "" {
	  requests = strings.Split(*optLL, ",")
  }
  if len(requests) > 0 {
	  //<<Iterate over requested levels, Pr. \ref{pr:dr}>>
  } else {
	  levels = knowns
  }
#+end_src
#+begin_export latex
We import \ty{strings}.
#+end_export
#+begin_src go <<Imports, Pr. \ref{pr:dr}>>=
  "strings"
#+end_src
#+begin_export latex
We fill in the known assembly levels.
#+end_export
#+begin_src go <<Fill in known levels, Pr. \ref{pr:dr}>>=
  for _, level := range tdb.AssemblyLevels() {
	  knowns[level] = true
  }
#+end_src
#+begin_export latex
We import \ty{tdb}.
#+end_export
#+begin_src go <<Imports, Pr. \ref{pr:dr}>>=
  "github.com/evolbioinf/neighbors/tdb"
#+end_src
#+begin_export latex
When iterating over the requested levels we store those that are
known. If we come across an unknown level, something has gone wrong
and we bail with a message.
#+end_export
#+begin_src go <<Iterate over requested levels, Pr. \ref{pr:dr}>>=
  for _, request := range requests {
	  if knowns[request] {
		  levels[request] = true
	  } else {
		  log.Fatalf("unknown level %q", request)
	  }
  }
#+end_src
#+begin_export latex
If the maximum tree level is less than zero, we return all levels by
setting the option to the largest integer.
#+end_export
#+begin_src go <<Respond to \ty{-m}, Pr. \ref{pr:dr}>>=
  if *optM < 0 {
	  *optM = math.MaxInt
  }
#+end_src
#+begin_export latex
We import \ty{math}.
#+end_export
#+begin_src go <<Imports, Pr. \ref{pr:dr}>>=
  "math"
#+end_src
#+begin_src latex
  The remaining two tokens on the command line are interpreted as
  taxon-ID and database, in that order. If we don't have two tokens, we
  bail with a friendly message. We convert the taxonomy-ID to an
  integer.
#+end_src
#+begin_src go <<Get taxon-ID, Pr. \ref{pr:dr}>>=
  tokens := flag.Args()
  if len(tokens) != 2 {
	  fmt.Fprintf(os.Stderr,
		  "please provide a taxon-ID and a database\n")
	  os.Exit(0)
  }
  tid, err := strconv.Atoi(tokens[0])
  if err != nil {
	  log.Fatalf("couldn't convert %q", tokens[0])
  }
#+end_src
#+begin_src latex
  We import \ty{fmt}, \ty{strconv}, \ty{os}, and \ty{log}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:dr}>>=
  "fmt"
  "strconv"
  "os"
  "log"
#+end_src
#+begin_src latex
  The database name is the second token.
#+end_src
#+begin_src go <<Get database name, Pr. \ref{pr:dr}>>=
  dbname := tokens[1]
#+end_src
#+begin_src latex
  We open a connection to the taxonomy database.
#+end_src
#+begin_src go <<Open database connection, Pr. \ref{pr:dr}>>=
  neidb, err := tdb.OpenTaxonomyDBcheck(dbname)
  util.Check(err)
#+end_src
#+begin_src latex
  We get the subtree we're looking for.
#+end_src
#+begin_src go <<Get subtree, Pr. \ref{pr:dr}>>=
  subtree, err := neidb.SubtreeLevel(tid, *optM)
  util.Check(err)
#+end_src
#+begin_src latex
  We annotate each node, $v$, of our subtree with two kinds of
  information: whether or not $v$ has at least one genome associated
  with it, and whether or not $v$ has at least one genome in the subtree
  rooted on it. So we construct two maps for storing this
  information. Then we mark nodes with genomes, and nodes with genomes
  in subtree.
#+end_src
#+begin_src go <<Annotate subtree, Pr. \ref{pr:dr}>>=
  hasGenome := make(map[int]bool)
  hasGsub := make(map[int]bool)
  //<<Mark nodes with genomes, Pr. \ref{pr:dr}>>
  //<<Mark nodes with genomes in subtree, Pr. \ref{pr:dr}>>
#+end_src
#+begin_src latex
We iterate over the nodes and mark those with genomes. These nodes are
also the initial set of nodes with genomes in their subtree.
#+end_src
#+begin_src go <<Mark nodes with genomes, Pr. \ref{pr:dr}>>=
  for _, v := range subtree {
	  acc, err := neidb.Accessions(v)
	  util.Check(err)
	  acc, err = neidb.FilterAccessions(acc, levels)
	  util.Check(err)
	  if len(acc) > 0 {
		  hasGenome[v] = true
		  hasGsub[v] = true
	  }
  }
#+end_src
#+begin_src latex
  To find the nodes with genomes in their subtrees, we iterate over the
  nodes and for each node with a genome in it subtree, we propagate this
  fact up the tree.
#+end_src
#+begin_src go <<Mark nodes with genomes in subtree, Pr. \ref{pr:dr}>>=
  for _, v := range subtree {
	  if hasGsub[v] {
		  //<<Propagate "has genome" up tree, Pr. \ref{pr:dr}>>
	  }
  }
#+end_src
#+begin_export latex
We climb up from the present node until we reach the root of the
subtre and mark the fact that the node has at least one genome in its
subtree.
#+end_export
#+begin_src go <<Propagate "has genome" up tree, Pr. \ref{pr:dr}>>=
  u := v
  p, err := neidb.Parent(u)
  util.Check(err)
  for u != tid {
	  hasGsub[p] = true
	  u = p
	  p, err = neidb.Parent(u)
	  util.Check(err)
  }
#+end_src
#+begin_src latex
Before we draw the subtree, we ensure stable output by sorting the
integers that make up the subtree. Then we draw the subtree graph,
unless the user has opted for listing the taxa in the subtree.
#+end_src
#+begin_src go <<Draw subtree, Pr. \ref{pr:dr}>>=
  sort.Ints(subtree)
  if *optL {
	  //<<List subtree, Pr. \ref{pr:dr}>>
  } else {
	  //<<Draw subtree graph, Pr. \ref{pr:dr}>>
  }
#+end_src
#+begin_src latex
We import \ty{sort}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:dr}>>=
  "sort"
#+end_src
#+begin_src latex
  We list the subtree in a table consisting of four mandatory columns,
  taxon-ID, parent, rank, and the number of genomes. In addition, the
  user might have opted for names. We construct this table with a
  \ty{tabwriter}. Then we iterate over the nodes and list each one. At
  the end we flush the tabwriter.
#+end_src
#+begin_src go <<List subtree, Pr. \ref{pr:dr}>>=
  w := tabwriter.NewWriter(os.Stdout, 0, 1, 2, ' ', 0)
  fmt.Fprint(w, "# Taxid\tParent\tRank\tGenomes")
  if *optN {
	  fmt.Fprint(w, "\tName")
  }
  fmt.Fprint(w, "\n")
  for _, v := range subtree {
	  //<<List one node, Pr. \ref{pr:dr}>>
  }
  w.Flush()
#+end_src
#+begin_src latex
We import \ty{tabwriter}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:dr}>>=
  "text/tabwriter"
#+end_src
#+begin_src latex
  We count the number of genomes associated with the current node. Then
  we consider whether the user opted to list only taxa with genomes.
#+end_src
#+begin_src go <<List one node, Pr. \ref{pr:dr}>>=
  numGenomes := 0
  //<<Count genomes, Pr. \ref{pr:dr}>>
  if !*optG || numGenomes > 0 {
  	  //<<Print node line, Pr. \ref{pr:dr}>>
  }
#+end_src
#+begin_export latex
If the user requested recursive genome counts, we provide that.
#+end_export
#+begin_src go <<Count genomes, Pr. \ref{pr:dr}>>=
  for level, _ := range levels {
  	n := 0
  	var err error
  	if *optRR {
  		n, err = neidb.NumGenomesRec(v, level)
  		util.Check(err)
  	} else {
  		n, err = neidb.NumGenomes(v, level)
  	}
  	numGenomes += n
  }
#+end_src
#+begin_export latex
We write the current node into a line consisting of taxon-ID, parent,
rank, and number of genomes. If the user opted for names, we print
them as the fifth column.
#+end_export
#+begin_src go <<Print node line, Pr. \ref{pr:dr}>>=
  p, err := neidb.Parent(v)
  util.Check(err)
  r, err := neidb.Rank(v)
  util.Check(err)
  fmt.Fprintf(w, "%d\t%d\t%s\t%d", v, p, r, numGenomes)
  if *optN {
	  a, err := neidb.Name(v)
	  util.Check(err)
	  fmt.Fprintf(w, "\t%s", a)
  }
  fmt.Fprintf(w, "\n")
#+end_src
#+begin_src latex
  The default node is simply its taxon-ID. However, nodes can be
  annotated with color and a taxon name. For this purpose we declare
  templates for annotation lines, one for color, ``lightsalmon'', the
  other for the name.

  Then we draw the header and footer of the graph, and the actual
  subtree sandwiched in between. To improve legibility, that subtree is
  oriented left to right rather than the default top to bottom. For each
  node $v$ in the subtree, we print $v$ and its parent.
#+end_src
#+begin_src go <<Draw subtree graph, Pr. \ref{pr:dr}>>=
  t1 := "\t%d [color=\"lightsalmon\",style=filled]\n"
  t2 := "\t%d [label=\"%s\"]\n"
  fmt.Printf("digraph g {\n\trankdir=LR\n")
  for _, v := range subtree {
	  //<<Print $v$ and its parent, Pr. \ref{pr:dr}>>
  }
  fmt.Printf("}\n")
#+end_src
#+begin_src latex
  We print a node, $v$, if one of two conditions holds. Either the user
  did not restrict the output to genomes-only, or the user did make this
  restriction and $v$ has a genome in its subtree. If $v$ isn't the root
  of the subtree, we look up its parent, $p$. If $p$ isn't identical to
  $v$, in other words, if $v$ is not the global root, we also print $p$
  and the connecting edge.
#+end_src
#+begin_src go <<Print $v$ and its parent, Pr. \ref{pr:dr}>>=
  if !*optG || (*optG && hasGsub[v]) {
	  //<<Print $v$, Pr. \ref{pr:dr}>>
	  if v != tid {
		  p, err := neidb.Parent(v)
		  util.Check(err)
		  if p != v {
			  fmt.Printf("\t%d -> %d\n", p, v)
		  }
	  }
  }
#+end_src
#+begin_src latex
  If the current node has a genome attached, we print a colored node,
  otherwise we print a black-and-white node. If the user asked for
  names, we also add a name attribute.
#+end_src
#+begin_src go <<Print $v$, Pr. \ref{pr:dr}>>=
  if hasGenome[v] {
	  fmt.Printf(t1, v)
  } else {
	  fmt.Printf(t2, v, strconv.Itoa(v))
  }
  if *optN {
	  name, err := neidb.Name(v)
	  util.Check(err)
	  fmt.Printf(t2, v, name)
  }
#+end_src
#+begin_src latex
  We've finished writing \ty{dree}, now we test it.

  \section*{Testing}
  Our outline for testing \ty{dree} contains hooks for imports and the
  testing logic.
#+end_src
#+begin_src go <<dree_test.go>>=
  package dree

  import (
	  "testing"
	  //<<Testing imports, Pr. \ref{pr:dr}>>
  )

	//<<Testing types, Pr. \ref{pr:dr}>>

  func TestDree(t *testing.T) {
	  //<<Testing, Pr. \ref{pr:dr}>>
  }
#+end_src
#+begin_src latex
  A test is composed of a command and an expected result.
#+end_src
#+begin_src go <<Testing types, Pr. \ref{pr:dr}>>=
  type Test struct {
    t *exec.Cmd
    r int
  }
#+end_src
#+begin_src latex
  We import \ty{exec}.
#+end_src
#+begin_src go <<Testing imports, Pr. \ref{pr:dr}>>=
  "os/exec"
#+end_src
#+begin_src latex
  We construct tests and run them in a loop.
#+end_src
#+begin_src go <<Testing, Pr. \ref{pr:dr}>>=
  var tests []Test
  //<<Construct tests, Pr. \ref{pr:dr}>>
  for _, test := range tests {
	  //<<Run test, Pr. \ref{pr:dr}>>
  }
#+end_src
#+begin_src latex
  In our tests, we draw the \emph{Homininae} which have taxon-ID
  207598. First with no options, then we test our six options names
  (\ty{-n}), genomes (\ty{-g}), list \ty{-l}, assembly level (\ty{-L}),
  tree level, \ty{-m}, and recursive genome count, \ty{-R}. We run all the test
  both locally and remotely.
#+end_src
#+begin_src go <<Construct tests, Pr. \ref{pr:dr}>>=
  n := "207598"
  d := "../data/test.db"
  p := "./cmd/dree"
  //<<Test without options, Pr. \ref{pr:dr}>>
  //<<Test \ty{-n}, Pr. \ref{pr:dr}>>
  //<<Test \ty{-g}, Pr. \ref{pr:dr}>>
  //<<Test \ty{-l}, Pr. \ref{pr:dr}>>
  //<<Test \ty{-L}, Pr. \ref{pr:dr}>>
  //<<Test \ty{-m}, Pr. \ref{pr:dr}>>
  //<<Test \ty{-R}, Pr. \ref{pr:dr}>>
#+end_src
#+begin_export latex
We construct our first test without any options and store it.
#+end_export
#+begin_src go <<Test without options, Pr. \ref{pr:dr}>>=
  test := Test{t: exec.Command(p, n, d), r: 1}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", n, d), r: 1}
  tests = append(tests, test)
#+end_src
#+begin_export latex
We test the names option, \ty{-n}.
#+end_export
#+begin_src go <<Test \ty{-n}, Pr. \ref{pr:dr}>>=
  test = Test{t: exec.Command(p, "-n", n, d), r: 2}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-n", n, d), r: 2}
  tests = append(tests, test)
#+end_src
#+begin_export latex
We test the genomes option, \ty{-g}.
#+end_export
#+begin_src go <<Test \ty{-g}, Pr. \ref{pr:dr}>>=
  test = Test{t: exec.Command(p, "-g", n, d), r: 3}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-g", n, d), r: 3}
  tests = append(tests, test)
#+end_src
#+begin_export latex
We test the list option, \ty{-l}, alone and in combination with genome
and name.
#+end_export
#+begin_src go <<Test \ty{-l}, Pr. \ref{pr:dr}>>=
  test = Test{t: exec.Command(p, "-l", n, d), r: 4}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-l", n, d), r: 4}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-l", "-g", n, d), r: 5}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-l", "-g", n, d), r: 5}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-l", "-n", n, d), r: 6}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-l", "-n", n, d), r: 6}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-l", "-g", "-n", n, d), r: 7}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-l", "-g", "-n", n, d), r: 7}
  tests = append(tests, test)
#+end_src
#+begin_export latex
We test \ty{-L}, first with level \emph{complete}, then with levels
\emph{complete} and \emph{chromosome}.
#+end_export
#+begin_src go <<Test \ty{-L}, Pr. \ref{pr:dr}>>=
  test = Test{t: exec.Command(p, "-L", "complete", n, d), r: 8}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-L", "complete", n, d), r: 8}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-L", "complete,chromosome", n, d), r: 9}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-L", "complete,chromosome", n, d), r: 9}
  tests = append(tests, test)
#+end_src
#+begin_export latex
We test \ty{-m} with a negative maximum level, ordinary levels 0 and
2, and the very large level 200.
#+end_export
#+begin_src go <<Test \ty{-m}, Pr. \ref{pr:dr}>>=
  test = Test{t: exec.Command(p, "-m", "-2", n, d), r: 10}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-m", "-2", n, d), r: 10}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-m", "0", n, d), r: 11}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-m", "0", n, d), r: 11}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-m", "2", n, d), r: 12}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-m", "2", n, d), r: 12}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-m", "200", n, d), r: 13}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-m", "200", n, d), r: 13}
  tests = append(tests, test)
#+end_src
#+begin_export latex
We test recursive genome counting, \ty{-R}, in conjunction with
listing.
#+end_export
#+begin_src go <<Test \ty{-R}, Pr. \ref{pr:dr}>>=
  test = Test{t: exec.Command(p, "-R", "-l", n, d), r: 14}
  tests = append(tests, test)
  test = Test{t: exec.Command(p, "-D", "test", "-R", "-l", n, d), r: 14}
  tests = append(tests, test)
#+end_src
#+begin_src latex
  For each test we compare the output we get with the output we want,
  which is contained in \ty{r1.txt}, \ty{r2.txt}, and so on.
#+end_src
#+begin_src go <<Run test, Pr. \ref{pr:dr}>>=
  get, err := test.t.Output()
  if err != nil {
	  t.Errorf("couldn't run %q", test.t)
  }
  f := "r" + strconv.Itoa(test.r) + ".txt"
  want, err := ioutil.ReadFile(f)
  if err != nil {
	  t.Errorf("couldn't open %q", f)
  }
  if !bytes.Equal(get, want) {
	  t.Errorf("get:\n%s\nwant:\n%s", get, want)
  }
#+end_src
#+begin_src latex
  We import \ty{strconv}, \ty{ioutil}, and \ty{bytes}.
#+end_src
#+begin_src go <<Testing imports, Pr. \ref{pr:dr}>>=
  "strconv"
  "io/ioutil"
  "bytes"
#+end_src
