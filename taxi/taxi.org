#+begin_src latex
  \section*{Introduction}
  The \ty{neighbors} module is based on taxon-IDs. These are difficult
  to remember, so the program \ty{taxi} takes the user from the name of
  an organism to its taxon-ID. It can also take the user on the return
  trip from a taxon-ID to the scientific name. The input to \ty{taxi} is
  thus either a taxon-ID or a scientific name, in addition to the
  taxonomy database.

  If the user supplied a taxon-ID, \ty{taxi} simply looks up the
  corresponding scientific name. If the user supplied a scientific name,
  the search is a bit more involved. To start with, it is always
  case-insensitive, and by default a query is bracketed by wildcards
  together with its internal blanks. For example,
  \begin{verbatim}
  a b
  \end{verbatim}
  becomes
  \begin{verbatim}
  %a% %b%
  \end{verbatim}
  The outer wild cards allow substring matching to find a string like
  \verb+xa by+. Bracketing blanks by wild cards allows matches to
  strings in square brackets, like \verb+xa [y]+. This accounts for the
  policy of the NCBI to use square brackets to indicate
  misclassification. For example, the species \emph{Haemophilus
  ducreyi} is currently stored as the string \ty{[Haemophilus]
    ducreyi}. This seems to mainly affect genus names, but not only, as
  shown by the name \ty{Pseudomonas [fluorescens] ATCC 17400}.

  Instead of this default substring matching, the user can opt for exact
  matching, in which case no wild cards are inserted. Regardless of the
  query type, there may be more than one, and potentially very many,
  matches. In such cases, paged data retrieval may be more convenient
  than wholesale output of all matches at once. To allow paged data
  retrieval, the user can set a limit on the number of names returned
  and an offset into the results list.

  \section*{Implementation}
  The program \ty{taxi} has hooks for imports and the logic of the run
  function.\bpr{taxi}{pr:tax}
#+end_src
#+begin_src go <<taxi.go>>=
  package taxi

  import (
	  //<<Imports, Pr. \ref{pr:tax}>>
  )

  func Run() {
	  //<<Run function, Pr. \ref{pr:tax}>>
  }
#+end_src
#+begin_src latex
  \epr In the \ty{Run} function we set the name of \ty{taxi} and its
  usage, declare the options, parse the options, get the taxon-IDs, and
  print them.
#+end_src
#+begin_src go <<Run function, Pr. \ref{pr:tax}>>=
  util.SetName("taxi")
  //<<Set usage, Pr. \ref{pr:tax}>>
  //<<Declare options, Pr. \ref{pr:tax}>>
  //<<Parse options, Pr. \ref{pr:tax}>>
  //<<Get taxon-IDs, Pr. \ref{pr:tax}>>
  //<<Print taxon-IDs, Pr. \ref{pr:tax}>>
#+end_src
#+begin_export latex
We import \ty{util}.
#+end_export
#+begin_src go <<Imports, Pr. \ref{pr:tax}>>=
  "github.com/evolbioinf/neighbors/util"
#+end_src
#+begin_src latex
  The usage consists of three parts: The actual usage message, an
  explanation of the purpose of \ty{taxi}, and an example command.
#+end_src
#+begin_src go <<Set usage, Pr. \ref{pr:tax}>>=
  u := "taxi [option] <scientific-name|taxid> <db>"
  p := "Take user from scientific name to taxon-ID " +
	  "or vice versa."
  e := "taxi \"homo sapiens\" neidb"
  clio.Usage(u, p, e)
#+end_src
#+begin_src latex
  We import \ty{clio}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:tax}>>=
  "github.com/evolbioinf/clio"
#+end_src
#+begin_src latex
  Apart from the version, we declare options for exact matching, limit,
  offset, and using a taxon-ID. The defaults are substring matching, no
  limit, no offset, and scientific name.
#+end_src
#+begin_src go <<Declare options, Pr. \ref{pr:tax}>>=
  var optV = flag.Bool("v", false, "version")
  var optE = flag.Bool("e", false, "exact match")
  var optL = flag.Int("l", -1, "limit output to <= l taxids")
  var optO = flag.Int("o", 0, "offset into taxid list")
  var optT = flag.Bool("t", false, "taxid instead of name")
  var optR = flag.String("r", "", "name of remote database (implies remote execution)")
#+end_src
#+begin_src latex
  We import \ty{flag}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:tax}>>=
  "flag"
#+end_src
#+begin_src latex
  We parse the options and respond to the version option (\ty{-v})
  first, as this would stop the program. If (\ty{-r}) contains a remote 
  database, we need to act on it, before inspecting the remaining
  arguments. Then we get the remaining arguments and count them to make sure
  there are two of them, the taxon label and the database, which we both store.
  From the taxon label we extract either the taxon name of the taxon-ID.
#+end_src
#+begin_src go <<Parse options, Pr. \ref{pr:tax}>>=
  flag.Parse()
  //<<Respond to \ty{-v}, Pr. \ref{pr:tax}>>
  //<<Respond to \ty{-r}, Pr. \ref{pr:tax}>>
  args := flag.Args()
  //<<Count arguments, Pr. \ref{pr:tax}>>
  label := args[0]
  db := args[1]
  //<<Extract taxon name or ID, Pr. \ref{pr:tax}>>
#+end_src
#+begin_export latex
If the user asked for the version, we print the program information,
which contains the version.
#+end_export
#+begin_src go <<Respond to \ty{-v}, Pr. \ref{pr:tax}>>=
  if *optV {
  	  util.PrintInfo("taxi")
  }
#+end_src
#+begin_src latex
  The remote switch is an option to execute \ty{taxi} on a server instead of
  locally. For this we need to call \ty{SendGetRequest} and provide the call
  arguments. The request url path is \ty{api/v2/programs/taxi}.
#+end_src
#+begin_src go <<Respond to \ty{-r}, Pr. \ref{pr:tax}>>=
  if *optR != "" {
    resp := util.SendGetRequest(
      "api/v2/programs/taxi",
      os.Args[1:],
      []string{},
      map[string]string{"db": *optR},
    )
    fmt.Print(resp)
    return
  }
#+end_src
#+begin_export latex
  Back to local execution.  If the user didn't provide two arguments, we don't
  have the necessary taxon and a database to work with, so we send a friendly
  message and quit.
#+end_src
#+begin_src go <<Count arguments, Pr. \ref{pr:tax}>>=
  m := "please provide a taxon and a database"
  if len(args) != 2 {
	  fmt.Fprintf(os.Stderr, "%s\n", m)
	  os.Exit(1)
  }
#+end_src
#+begin_src latex
  We import \ty{fmt} and \ty{os}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:tax}>>=
  "fmt"
  "os"
#+end_src
#+begin_export latex
If the user supplied a taxon-ID, we know we are dealing with an
integer. Otherwise the label is a string containing the scientific
name, which we need to still prepare.
#+end_export
#+begin_src go <<Extract taxon name or ID, Pr. \ref{pr:tax}>>=
  name := ""
  taxid := 0
  if *optT {
	  x, err := strconv.ParseInt(label, 0, 0)
	  util.Check(err)
	  taxid = int(x)
  } else {
	  name = label
	  //<<Prepare name, Pr. \ref{pr:tax}>>
  }
#+end_src
#+begin_export latex
We import \ty{strconv}.
#+end_export
#+begin_src go <<Imports, Pr. \ref{pr:tax}>>=
  "strconv"
#+end_src
#+begin_export latex
To prepare the scientific name for searching, we consider whether the
user chose exact matching, in which case we take the label as
given. Otherwise, we summarize consecutive white space into single
blanks. Then we bracket the taxon name and any remaining blanks with
wild cards.
#+end_export
#+begin_src go <<Prepare name, Pr. \ref{pr:tax}>>=
  if !*optE {
	  na := strings.Fields(name)
	  name = strings.Join(na , "% %")
	  name = "%" + name + "%"
  }
#+end_src
#+begin_src latex
  We import \ty{strings}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:tax}>>=
  "strings"
#+end_src
#+begin_src latex
  Our output is based on a slice of taxon-IDs, \ty{taxa}. If the user
  supplied a taxon-ID, that is the only entry in \ty{taxa}.  If, on the
  other hand, the user supplied a name, we get the corresponding zero,
  one, or more taxon-IDs through a method call on the taxonomy
  database. If we don't find any taxa, we're done.
#+end_src
#+begin_src go <<Get taxon-IDs, Pr. \ref{pr:tax}>>=
  taxdb, err := tdb.OpenTaxonomyDBcheck(db)
  util.Check(err)
  taxa := []int{taxid}
  if !*optT {
	  taxa, err = taxdb.Taxids(name, *optL, *optO)
	  util.Check(err)
  }
  if len(taxa) == 0 {
	  return
  }
#+end_src
#+begin_src latex
  We import \ty{tdb}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:tax}>>=
  "github.com/evolbioinf/neighbors/tdb"
#+end_src
#+begin_src latex
  We print the taxon-IDs, the parents' IDs, and the corresponding names
  in a table. We layout the table using a \ty{tabwriter}.
#+end_src
#+begin_src go <<Print taxon-IDs, Pr. \ref{pr:tax}>>=
  w := tabwriter.NewWriter(os.Stdout, 0, 1, 2, ' ', 0)
  defer w.Flush()
  fmt.Fprintf(w, "# ID\tParent\tName\n")
  for _, taxon := range taxa {
	  name, err := taxdb.Name(taxon)
	  util.Check(err)
	  p, err := taxdb.Parent(taxon)
	  util.Check(err)
	  fmt.Fprintf(w, "  %d\t%d\t%s\n", taxon, p, name)
  }
#+end_src
#+begin_src latex
  We import \ty{tabwriter}.
#+end_src
#+begin_src go <<Imports, Pr. \ref{pr:tax}>>=
  "text/tabwriter"
#+end_src
#+begin_src latex
  We're done writing \ty{taxi}, time to test it.

  \section*{Testing}
  Our testing code for \ty{taxi} contains hooks for imports and the
  testing logic.
#+end_src
#+begin_src go <<taxi_test.go>>=
  package taxi

  import (
	  "testing"
	  //<<Testing imports, Pr. \ref{pr:tax}>>
  )

  func TestTaxi(t *testing.T) {
	  //<<Testing, Pr. \ref{pr:tax}>>
  }
#+end_src
#+begin_src latex
  We construct the tests and iterate over them.
#+end_src
#+begin_src go <<Testing, Pr. \ref{pr:tax}>>=
  var tests []*exec.Cmd
  //<<Construct tests, Pr. \ref{pr:tax}>>
  for i, test := range tests {
	  //<<Run test, Pr. \ref{pr:tax}>>
  }
#+end_src
#+begin_src latex
  We import \ty{exec}.
#+end_src
#+begin_src go <<Testing imports, Pr. \ref{pr:tax}>>=
  "os/exec"
#+end_src
#+begin_export latex
Our first query is 9606, the taxon-ID for human.
#+end_export
#+begin_src go <<Construct tests, Pr. \ref{pr:tax}>>=
  db := "../data/test.db"
  p := "./cmd/taxi"
  test := exec.Command(p, "-t", "9606", db)
  tests = append(tests, test)
#+end_src
#+begin_src latex
  Our next four queries are \emph{Homo sapiens}, \emph{Homo\_\_sapiens}
  with two blanks in the middle, \emph{Haemophilus ducreyi}, and
  \emph{Pseudomonas fluorescens}. We run these queries in default mode
  and in exact mode. Remember that string matching in SQL databases is
  case-insensitive.
#+end_src
#+begin_src go <<Construct tests, Pr. \ref{pr:tax}>>=
  taxa := []string{"homo sapiens",
	  "homo  sapiens",
	  "haemophilus ducreyi",
	  "pseudomonas fluorescens ATCC 17400"}
  for _, taxon := range taxa {
	  test := exec.Command(p, taxon, db)
	  tests = append(tests, test)
	  test = exec.Command(p, "-e", taxon, db)
	  tests = append(tests, test)
  }
#+end_src
#+begin_src latex
  We execute the test and compare the result we get to the result we
  want. The results we want are contained in the files \ty{r1.txt}, 
  \ty{r2.txt} and so on.
#+end_src
#+begin_src go <<Run test, Pr. \ref{pr:tax}>>=
  get, err := test.Output()
  if err != nil { t.Error(err) }
  f := "r" + strconv.Itoa(i + 1) + ".txt"
  want, err := ioutil.ReadFile(f)
  if err != nil { t.Error(err) }
  if !bytes.Equal(get, want) {
  	  t.Errorf("%s - get:\n%s\nwant:\n%s\n", f, get, want)
  }
#+end_src
#+begin_src latex
  We import \ty{strconv}, \ty{ioutil}, and \ty{bytes}.
#+end_src
#+begin_src go <<Testing imports, Pr. \ref{pr:tax}>>=
  "strconv"
  "io/ioutil"
  "bytes"
#+end_src

